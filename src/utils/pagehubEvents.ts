/**
 * App-wide CustomEvent names used across SDK runtime and app hooks.
 * Keep in one place so listeners and dispatchers stay aligned.
 */
export const PAGEHUB_URL_QUERY_CHANGED_EVENT = "pagehub:url-query-changed";
