/**
 * Keyboard utility barrel — canonical predicates and helpers used by the
 * registry dispatcher and any surface that still mounts its own listeners
 * during the Wave B/Phase 2 transition.
 */
export { isInsideTextEditingSurface } from "./isInsideTextEditingSurface";
